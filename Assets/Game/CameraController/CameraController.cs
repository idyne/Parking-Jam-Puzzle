using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //transform.position = Road.Instance.origin - 40 *(Road.Instance.lastBuiltRoadParkingLotLength / 15f) * transform.forward - (Road.Instance.lastBuiltRoadParkingLotLength / 15f) * 3 * transform.up + (Road.Instance.lastBuiltRoadParkingLotWidth / 20f) * 1 * transform.right;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
